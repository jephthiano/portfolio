//export const server = "http://www.portfoliobe.com/"

export const server = "https://jephthiano.000webhostapp.com/"

export const imgServer = `${server}media/`;

export const err_msg = "<b class='j-text-color8'>Sorry!!!</b><br>";

export const root_url = window.location.origin;

export const page_absolute = window.location;

export const page_url = window.location.href;

export const page_protocol = window.location.protocol;

export const page_host = window.location.host;

export const page_hostname = window.location.hostname;

export const page_pathname = window.location.pathname;

export const page_search = window.location.search;

export const page_hash = window.location.hash;

export const page_port = window.location.port;